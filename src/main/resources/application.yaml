server:
  servlet:
    context-path: /api/payroll

spring:
  application:
    name: wos-payroll
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 25MB
      max-request-size: 25MB
